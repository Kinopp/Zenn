---
title: "Iteratorパターン"
free: false
---

## 概要



## クラス構造
![](https://storage.googleapis.com/zenn-user-upload/c30291c33e45-20260405.png =500x)


## イメージ
【効果】
Iteratorを使用することで繰り返しの実装の際、変数のインクリメントやフラグ操作などを行う必要がない
![](https://storage.googleapis.com/zenn-user-upload/7778613b4caa-20260405.png =580x)

## メリット




## デメリット



## サンプルソース


```Java:Iterator.java
public interface Iterator {
    public boolean hasNext();
    public Object next();
}
```
```Java:ConcreteIterator.java
public class ConcreteIterator implements Iterator {
    private ConcreteAggregate aggregate;
    private int index;
    
    public ConcreteIterator(ConcreteAggregate aggregate){
        this.aggregate = aggregate;
        this.index = 0;
    }
    
    @Override
    public boolean hasNext() {
        return index < aggregate.getLength();
    }

    @Override
    public Object next() {
        Content content = aggregate.getContentAt(index);
        index++;
        return content;
    }
}
```
```Java:Aggregate.java
public interface Aggregate {
    public Iterator iterator();
}
```
```Java:ConcreteAggregate.java
import java.util.ArrayList;
import java.util.List;

public class ConcreteAggregate implements Aggregate{
    private List contents;
    
    public ConcreteAggregate(){
        this.contents = new ArrayList();
    }
    
    public Content getContentAt(int index){
        return (Content)contents.get(index);
    }
    
    public void appendContent(Content content){
        this.contents.add(content);
    }
    
    public int getLength(){
        return contents.size();
    }
    
    @Override
    public Iterator iterator() {
        return new ConcreteIterator(this);
    }
}
```
```Java:Content.java
public class Content {
    private String name;
    
    public Content(String name){
        this.name = name;
    }
    
    public String getName(){
        return name;
    }
}
```
```Java:Client.java
public class Client {
    public static void main(String... args){
        ConcreteAggregate concreteAggregate = new ConcreteAggregate();
        
        concreteAggregate.appendContent(new Content("１番目のコンテント"));
        concreteAggregate.appendContent(new Content("２番目のコンテント"));
        concreteAggregate.appendContent(new Content("３番目のコンテント"));

        Iterator iterator = concreteAggregate.iterator();
        while(iterator.hasNext()){
            Content content = (Content)iterator.next();
            System.out.println(content.getName());
        }
    }
}
```


### 実行結果
```
１番目のコンテント
２番目のコンテント
３番目のコンテント
```


